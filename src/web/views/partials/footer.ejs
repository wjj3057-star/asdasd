  <% if (typeof syncMsg !== 'undefined' && syncMsg) { %>
  <script>
    (function(){
      var f = document.querySelector('form[action="/panel/refresh"]');
      // no-op placeholder for future toast handling
    })();
  </script>
  <% } %>
</main>
</body>
</html>
